import { FC } from "react";

const Slider: FC = () => {
  return (
    <div className="bg-gradient-to-tr from-[#1F1F20] to-[transparent] rounded-[22px] p-[32px] gap-[32px]"></div>
  );
};

export default Slider;
